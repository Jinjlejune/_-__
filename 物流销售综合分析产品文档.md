# 物流销售综合分析产品文档

## 1. 项目概述

本项目基于 `E:/animal/data_wuliu.csv`，使用 Python、pandas、Matplotlib 和 Jupyter Notebook，完成物流销售数据的清洗、规整、探索性分析与可视化。

目标是形成一套可重复运行的分析流程，输出清洗后的明细数据、关键指标、图表和质量检查结果，为销售经营与物流交付改进提供依据。

## 2. 数据概况

- 原始文件：`E:/animal/data_wuliu.csv`
- 文件编码：GB18030
- 原始记录：1161 条
- 原始字段：10 个
- 已发现问题：9 条完全重复记录、订单号缺失 2 条、交货状态缺失 2 条、数量缺失 4 条、交货状态存在前导空格、销售金额存在“元/万元”和逗号小数混用。

原始字段及含义：

| 原始字段 | 业务含义 | 标准字段 | 标准类型 |
| --- | --- | --- | --- |
| 订单号 | 订单标识 | `order_id` | string |
| 订单行 | 订单明细行号 | `order_line` | Int64 |
| 销售时间 | 销售日期 | `sale_date` | datetime64 |
| 交货时间 | 交货日期 | `delivery_date` | datetime64 |
| 货品交货状况 | 交付是否及时 | `delivery_status` | category/string |
| 货品 | 商品名称 | `product` | category/string |
| 货品用户反馈 | 用户反馈 | `customer_feedback` | category/string |
| 销售区域 | 销售区域 | `sales_region` | category/string |
| 数量 | 销售数量 | `quantity` | Float64 |
| 销售金额 | 销售金额 | `sales_amount_cny` | Float64 |

## 3. 数据清洗与规整规则

1. 使用 `encoding='gb18030'` 读取原始 CSV；保留原始文件不变。
2. 先删除原始数据中的 9 条完全重复副本，再将空字符串和全为空白的字符串统一为 `NaN`。
3. 所有文本字段去除前后空格；分类字段使用清洗后的值。
4. 完全重复记录保留首次出现记录；仅因空格差异造成的记录不在本轮重复删除范围内，但会通过字段标准化暴露给质量复核。
5. 将订单行、数量转换为数值类型；数量缺失保留为 `NaN`。
6. 将销售时间、交货时间转换为日期类型，检查交货时间不早于销售日期。
7. 将销售金额中的逗号转换为小数点，并把“万元”统一换算为人民币元；生成 `sales_amount_cny`。
8. 交货状态缺失统一填充为“未知”，同时在质量标记中保留缺失信息。
9. 订单号缺失记录保留并标记，不伪造订单号。
10. 对数量和金额执行业务规则检查：非数值、非正数、日期逻辑错误；再用 IQR 标记统计极端值，不默认删除真实业务极值。

新增分析字段：

- `delivery_days`：`delivery_date - sale_date` 的天数。
- `is_on_time`：交货状态为“按时交货”时为 True，晚交货为 False，未知为缺失。
- `data_quality_flag`：以分号连接的质量问题标记；无问题时为“正常”。

## 4. 分析指标与图表

### 核心指标

- 清洗后记录数、订单数
- 销售数量合计
- 销售金额合计
- 平均交货天数
- 按时交货率
- 用户反馈数量与占比

### 分析主题

- 按月份观察销售金额和销售数量趋势。
- 按销售区域比较金额、数量、平均交货天数和按时交货率。
- 按货品比较销售贡献和用户反馈结构。
- 分析按时/晚交货比例与交货天数分布。
- 分析用户反馈分布。
- 对比清洗前后的记录数、缺失值和重复记录。

### 可视化规范

所有图表使用 Matplotlib，统一设置中文字体候选列表、标题、坐标轴、图例和网格。图表保存到 `figures/`，格式为 PNG，分辨率 150 dpi。

## 5. 项目产物

- `物流销售综合分析产品文档.md`：产品与数据处理设计。
- `物流销售数据分析.ipynb`：可从头运行的交互式分析 Notebook。
- `analysis.py`：可复用的数据读取、清洗、指标和绘图函数。
- `requirements.txt`：运行依赖。
- `data_wuliu_cleaned.csv`：清洗后的标准化数据。
- `figures/`：分析图表。
- `quality_report.csv`：数据质量统计。
- `tests/`：单元测试和中文测试报告。

## 6. 运行方式

在目标目录执行：

```powershell
python -m pip install -r requirements.txt
python analysis.py
jupyter notebook
```

打开 `物流销售数据分析.ipynb`，选择 Python 内核并按顺序运行全部单元格。

运行测试：

```powershell
python -m pytest tests/ -v --tb=short
```

## 7. 验收标准

- 原始 CSV 不被覆盖。
- 清洗结果字段与标准字段一致，类型转换成功。
- 重复记录、缺失值、金额单位、空格和日期逻辑均有可追溯处理。
- Notebook 可从头到尾执行并生成清洗结果、质量报告和全部图表。
- 关键指标和分组结果可由 pandas 复算。
- pytest 单元测试和真实数据冒烟测试通过。

## 8. 默认假设

- 缺失数据保留并标记，不为缺失订单号和数量进行猜测填补。
- 异常值默认标记、不删除；是否排除由后续业务分析单独决定。
- “按时交货”按交货状态字段判断；缺失或未知状态不计入按时率分母。
- 金额统一为人民币元，原始金额文本不作为后续聚合字段。
