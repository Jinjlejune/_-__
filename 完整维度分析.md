# 物流配送完整维度分析

## 一、分析目标

本分析的目标是检查配送服务是否存在问题，并从四个角度找出延迟交货的规律：

1. 月份维度：不同月份的配送表现是否有明显差异。
2. 销售区域维度：哪个地区的延迟交货更严重。
3. 货品维度：哪种商品的延迟交货更多。
4. 货品与销售区域组合维度：具体哪个地区的哪种货品最需要排查。

## 二、分析前的数据处理

在分组前，必须清理“货品交货状况”的空格。

```python
# 将交货状态转换为字符串并去除首尾空格，避免同一状态被分成不同类别。
df['delivery_status'] = df['delivery_status'].astype('string').str.strip()

# 创建中文列名，后面的分组代码更容易理解。
df['货品交货状况'] = df['delivery_status']
```

例如，“ 按时交货”和“按时交货”原本会被 pandas 当成两个不同值；去掉空格后才可以正确合并统计。

## 三、统一指标：按时交货率

四个维度都使用同一个指标：

```text
按时交货率 = 按时交货数量 ÷ (按时交货数量 + 晚交货数量)
```

按时交货率越低，说明该维度的配送服务越需要关注。

Notebook 会先只保留“按时交货”和“晚交货”两种记录，再计算该指标。其他状态不进入分母，避免出现 `0 ÷ 0` 的无意义结果。

## 四、月份维度：检查季节性差异

```python
# 按“月份”和“货品交货状况”分组后统计数量。
date1 = df.groupby(['月份', '货品交货状况']).size().unstack()

# 计算每个月的按时交货率。
date1['按时交货率'] = date1['按时交货'] / (date1['按时交货'] + date1['晚交货'])
```

这里的 `groupby()` 是分组，`size()` 是计数，`unstack()` 会把“按时交货”和“晚交货”变成两列，因此可以直接计算比例。

如果某个月按时交货率明显较低，说明这个月需要结合天气、节假日、订单量和运力继续分析；它不能单独证明原因一定是季节。

## 五、销售区域维度：定位服务短板

```python
# 按“销售区域”和“货品交货状况”分组后统计数量。
date2 = df.groupby(['销售区域', '货品交货状况']).size().unstack()

# 计算每个区域的按时交货率。
date2['按时交货率'] = date2['按时交货'] / (date2['按时交货'] + date2['晚交货'])

# 从高到低排序，便于比较服务表现。
date2 = date2.sort_values('按时交货率', ascending=False)
```

排序后，最末尾的区域就是按时交货率最低的区域，应优先检查该地区的运力、路线、仓储衔接和天气等问题。

## 六、货品维度：定位单品问题

```python
# 按“货品”和“货品交货状况”分组后统计数量。
date3 = df.groupby(['货品', '货品交货状况']).size().unstack()

# 计算每种货品的按时交货率。
date3['按时交货率'] = date3['按时交货'] / (date3['按时交货'] + date3['晚交货'])
```

如果某种货品的按时交货率低，可能是该货品包装、库存、装卸或运输条件更特殊，需要业务人员进一步确认。

## 七、货品与销售区域组合维度：交叉定位问题

```python
# 同时按货品、销售区域和交货状态分组后统计数量。
date4 = df.groupby(['货品', '销售区域', '货品交货状况']).size().unstack()

# 计算每个“货品 + 销售区域”组合的按时交货率。
date4['按时交货率'] = date4['按时交货'] / (date4['按时交货'] + date4['晚交货'])

# 从低到高排序，让最需要关注的组合排在最前面。
date4 = date4.sort_values('按时交货率', ascending=True)
```

这个维度最具体。例如，若“货品 A + 华北”的按时交货率最低，说明问题不一定是所有货品或所有区域都有，而是集中在这个组合，应优先从该货品在华北的库存、路线和物流资源开始排查。

## 八、使用提醒

- 这四个分析用于发现问题线索，不会自动删除任何记录。
- 样本数量很少的分组，按时交货率容易大幅波动，不能只看比例，也要看“按时交货”和“晚交货”的数量。
- 找到低按时交货率后，应结合订单量、距离、天气、节假日和仓储信息判断真正原因。
