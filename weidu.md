# 按月配送维度分析说明

## 分析目的

按月份比较“按时交货”和“晚交货”的数量，计算每个月的按时交货率，初步观察季节变化是否可能影响配送表现。

## 使用的字段

- `月份`：从“销售时间”中提取的月份数字，例如 7 表示 7 月。
- `货品交货状况`：交货状态，包括“按时交货”和“晚交货”。

在分组前，Notebook 会使用 `.str.strip()` 去掉交货状态前后的空格，避免“ 按时交货”和“按时交货”被错误地统计为两个不同状态。

## 分析代码

```python
# 按“月份”和“货品交货状况”两个维度分组，并统计每组记录数量。
date1 = df.groupby(['月份', '货品交货状况']).size()

# 将两层索引展开成表格，使交货状态变成单独的列。
date1 = date1.unstack()

# 计算按时交货率：按时交货数量除以按时交货与晚交货数量之和。
date1['按时交货率'] = date1['按时交货'] / (date1['按时交货'] + date1['晚交货'])
```

## 小白理解

### 1. `groupby()`

把数据按“月份”和“交货状态”分组。例如，所有 7 月的“按时交货”记录是一组，所有 7 月的“晚交货”记录是另一组。

### 2. `size()`

统计每一组有多少条记录。例如，7 月“按时交货”有 80 条，7 月“晚交货”有 10 条。

### 3. `unstack()`

把原本上下排列的交货状态，转换成左右排列的列。这样可以直接得到“按时交货”列和“晚交货”列，后续计算更直观。

### 4. 按时交货率

计算公式：

```text
按时交货率 = 按时交货数量 ÷（按时交货数量 + 晚交货数量）
```

例如：某月按时交货 80 条、晚交货 20 条：

```text
按时交货率 = 80 ÷ (80 + 20) = 80%
```

## 如何解读

- 某个月按时交货率较低：说明这个月的配送表现相对较差，需要继续排查是否和季节、节假日、天气或运力有关。
- 某个月按时交货率较高：说明该月配送表现较稳定。
- 该分析只能说明不同月份存在差异，不能单独证明差异一定由季节造成；还需要结合天气、节假日和物流资源等业务信息判断。

---

# 按销售区域维度分析说明

## 分析目的

按销售区域统计“按时交货”和“晚交货”的数量，计算每个区域的按时交货率，再从高到低排序。按时交货率较低的区域，就是需要优先复核配送服务的区域。

## 分析代码

```python
# 按“销售区域”和“货品交货状况”两个维度分组，并统计每组记录数量。
date2 = df.groupby(['销售区域', '货品交货状况']).size().unstack()

# 计算各销售区域的按时交货率。
date2['按时交货率'] = date2['按时交货'] / (date2['按时交货'] + date2['晚交货'])

# 按按时交货率从高到低排序，便于比较各区域服务表现。
date2 = date2.sort_values('按时交货率', ascending=False)
```

## 小白理解

### 1. 分组维度的变化

按月分析使用的是“月份”；这里把分组维度换成“销售区域”。例如，把华北地区的“按时交货”和“晚交货”分别统计出来，再计算华北地区的按时交货率。

### 2. `sort_values(ascending=False)`

`sort_values()` 的作用是排序：

- `ascending=False`：从大到小排列，按时交货率最高的区域排在最上方。
- `ascending=True`：从小到大排列，按时交货率最低的区域排在最上方。

Notebook 按题目要求先做降序排列，同时使用 `idxmin()` 找到按时交货率最低的区域，作为优先关注的服务短板。

## 如何解读

- 按时交货率低的区域：可能存在运力不足、路线距离较远、仓储衔接不顺畅等问题，需要业务人员进一步排查。
- 按时交货率高的区域：配送表现相对稳定，可以作为其他区域改进时的参考。
- 该分析是定位线索，不能直接断定问题原因；还需要结合订单量、距离、天气和节假日等信息判断。
