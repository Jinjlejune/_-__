# 物流销售数据异常值识别说明

## 检查字段

- 销售数量：Notebook 中的 `quantity`
- 销售金额：Notebook 中的 `sales_amount_cny`，单位为人民币元；它对应需求中所说的 `sales_amount`

## 识别规则

### 1. 最小值异常

检查销售金额的最小值。

- 当最小值为 `0` 时，判定为“最小值异常”。
- 当最小值不为 `0` 时，不触发这一条规则。

金额为 0 可能表示免费订单、录入错误或缺少金额，因此需要结合业务进一步核实。

### 2. 标准差过大

分别检查销售数量和销售金额是否同时满足以下两个条件：

```python
标准差 > 均值 * 8
均值 > 中位数
```

两个条件都满足时，判定为“标准差过大异常”。

这表示数据波动特别大，而且少量较大的数值可能把平均数明显拉高。它是需要重点复核的信号，不代表这些大数值一定是错误数据。

## Notebook 中的实现

Notebook 的“异常值识别”单元会计算每个字段的：

- 最小值 `min`
- 均值 `mean`
- 中位数 `median`
- 标准差 `std`

然后显示判断结果，并输出三类结论：销售金额是否存在最小值为 0 的异常、销售数量是否标准差过大、销售金额是否标准差过大。

## 注意

本次功能只负责识别和报告异常值，不会自动删除异常记录。删除前应先确认异常值是录入错误，还是确实存在的大额订单或大批量订单。
