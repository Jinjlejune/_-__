# “月份”列添加说明

## 目的

为物流销售数据新增“月份”列，方便按月份汇总销售金额和销售数量，观察月度变化趋势。

## 操作步骤

Notebook 在数据清洗单元中依次执行以下代码：

```python
# 将“销售时间”列转换为标准的日期时间格式。
data['销售时间'] = pd.to_datetime(data['销售时间'])

# 对每条销售时间应用 lambda 表达式，提取月份数值并创建“月份”列。
data['月份'] = data['销售时间'].apply(lambda x: x.month)
```

## 代码含义

### 1. `pd.to_datetime()`

`pd.to_datetime()` 会将“销售时间”中的文本日期，例如 `2016-7-30`，转换为 pandas 能识别的日期时间格式。转换后才能方便地取得年份、月份和日期。

### 2. `apply(lambda x: x.month)`

`apply()` 会依次处理“销售时间”列中的每一条记录。

- `x` 表示当前正在处理的一条销售时间。
- `x.month` 表示取出这条日期中的月份数字。
- 结果写入新的“月份”列。

例如：

| 销售时间 | 月份 |
| --- | --- |
| 2016-07-30 | 7 |
| 2016-11-26 | 11 |
| 2017-01-26 | 1 |

## 后续使用

Notebook 的“月度趋势”步骤使用 `groupby('月份')`，按新增的“月份”列汇总销售金额和销售数量。
